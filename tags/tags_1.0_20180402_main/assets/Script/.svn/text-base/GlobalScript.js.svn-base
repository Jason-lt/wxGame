console.log("Global Script loaded");
ty = {};
hall = {};
ddz = {};

hall.LOGD = function(tag, msg) {
    var logStr = 'HALL : ' + msg;
    if (tag){
        logStr = tag + 'HALL : ' + msg;
    }
    console.log(logStr);
};

hall.LOGE = function(tag, msg) {
    var logStr = 'HALL : ' + msg;
    if (tag){
        logStr = tag + 'HALL : ' + msg;
    }
    console.error(logStr);
};